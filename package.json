{
  "name": "3allama",
  "version": "1.0.0",
  "description": "A web interface for Ollama with tool support and vision",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "stop": "pkill -f 'node server/index.js' || fuser -k 3000/tcp || true",
    "clean": "rm -f *.log server_*.log mock_*.log test_*.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
